name: CI


on:
  push:
    branches: [ "master" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - name: Setup pnpm
        uses: pnpm/action-setup@v2.2.2
        with:
          version: '7.11.0'

      - name: install deps
        run: pnpm install
        

      - name: copy dist to zzall.github.io
        run: |
          echo 到123
          
          
          
  Post:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: post to tencent centos services
        run: |
          echo ls之前：
          ls
          echo 之后的是：
          apt-get -v
          apt-get  install sshpass
          echo 已安装sshpass~
          # sshpass -p ${{ secrets.TENCENT_CENTOS_PASSWORD }} 
        
